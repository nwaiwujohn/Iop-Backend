define( [
	"../../var/pnum"
], function( pnum ) {
	return new RegExp( "^(" + pnum + ")(?!px)[a-z%]+$", "i" );
} );
	return new RegExp( "^(" + pnum + ")(?!px)[a-z%]+$", "i" );
define( [
	return new RegExp( "^(" + pnum + ")(?!px)[a-z%]+$", "i" );
	return new RegExp( "^(" + pnum + ")(?!px)[a-z%]+$", "i" );
