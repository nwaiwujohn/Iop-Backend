define( [
	"../Data"
], function( Data ) {
	return new Data();
} );
define( [
define( [
], function( Data ) {
], function( Data ) {
} );
], function( Data ) {
], function( Data ) {
	"../Data"
], function( Data ) {
